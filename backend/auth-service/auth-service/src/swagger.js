const swaggerUi =
    require(
        "swagger-ui-express"
    );

const swaggerSpec =
    require(
        "./config/swagger.config"
    );

module.exports = {


    swaggerUi,

    swaggerSpec


};
